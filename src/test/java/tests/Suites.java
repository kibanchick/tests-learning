package tests;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
        GoogleTest.class
})
public class Suites {
    // Тестовый набор для выполнения всех тестов
}
